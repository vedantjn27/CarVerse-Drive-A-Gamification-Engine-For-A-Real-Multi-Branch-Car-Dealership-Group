# Vercel production environment values for CarVerse Drive.
# Add these in Vercel: Project → Settings → Environment Variables → Production.
# They are public browser configuration values, not secrets.
VITE_API_BASE_URL=https://carverse-drive-a-gamification-engine-for.onrender.com
VITE_WS_BASE_URL=wss://carverse-drive-a-gamification-engine-for.onrender.com
